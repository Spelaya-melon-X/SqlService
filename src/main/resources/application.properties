spring.application.name=SqlService


# Kafka configuration
spring.kafka.bootstrap-servers=localhost:9092

# Data source configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/sqlservice
spring.datasource.username=myuser
spring.datasource.password=secret
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.mvc.pathmatch.use-suffix-pattern=false
spring.web.resources.add-mappings=false

## todo :  Test containers configuration
#sql-service.test-db.username=testuser
#sql-service.test-db.password=testpass
#sql-service.test-db.db-name=testdb

logging.level.org.apache.kafka=DEBUG
logging.level.org.springframework.kafka=DEBUG
logging.level.org.springframework.kafka.listener=DEBUG
logging.level.org.apache.kafka.clients.consumer=DEBUG